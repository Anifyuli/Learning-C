#include<stdio.h>

int main(){
    int radius = 7;
    float pi = 3.14;

    double circleWide = pi * radius * radius;

    printf("My circle wide is circle is %lf cm", circleWide);
    
    return 0;
}