#include <stdio.h>

int main() {
    int x = 6;
    int y = 2;

    printf("%d \n", x > y); // returns 1 (true) because 6 is greater than 2
    printf("%d \n", x < y); // returns 0 (false) because 6 is greater than 2

  return 0;
}